spring.application.name=carpool

# Database configuration
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:3306/carpool
spring.datasource.username=${MYSQL_USER}
spring.datasource.password=${MYSQL_PASSWORD}